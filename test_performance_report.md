# Test Performance Report

This report tracks `dotnet test` pass rate progression by attempt.

```mermaid
xychart-beta
    title "QC Test Pass Rate by Attempt"
    x-axis "Attempt" [1, 2, 3]
    y-axis "Pass Rate (%)" 0 --> 100
    line "Pass Rate" [0, 0, 100]
```
